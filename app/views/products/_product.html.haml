.product
  .product_id= product.item_num
  .product_image= link_to image_tag(product.medium_image), product_url(:id => product)
  .product_name= link_to product.name, product_url(:id => product)
  .product_price= display_product_price(product)\
  = add_to_cart_button(product)
