%h3 Upload Cart
%p
  you can upload a csv file (max 99 lines) that contains item_num and quantity.
%p 
  Sample csv format:
  %table{:style => "width:200px"}
    %tr
      %th item_num
      %th qty
    %tr
      %td 654324
      %td 5
    %tr
      %td 654325
      %td 7
    %tr
      %td 654326
      %td 3
%p
  = link_to 'Download sample csv file', :action => 'download_sample_cart_csv'
%h4 Upload Cart:
= form_tag fast_upload_path, :multipart => true, :method => :post, :id => 'upload' do
  = file_field_tag :file, :required => 'required'
  = submit_tag :upload
#uploading{:style=>"float:left;"}
  #progress
    #progressbar &nbsp;
    #percents
:javascript
  $(function() {
    $('form').uploadProgress({
      /* scripts locations for safari */
      jqueryPath: "http#{'s' if request.ssl?}://ajax.aspnetcdn.com/ajax/jQuery/jquery-1.5.2.min.js",
      uploadProgressPath: "/javascripts/jquery.uploadProgress.js",
      /* function called each time bar is updated */
      uploading: function(upload) {$('#percents').html(upload.percents+'%');},
      /* selector or element that will be updated */
      progressBar: "#progressbar",
      /* progress reports url */
      progressUrl: "/progress",
      /* how often will bar be updated */
      interval: 2000
     });
  });
