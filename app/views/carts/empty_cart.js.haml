== $('#shoppingcart_info').html('#{escape_javascript(render(:partial => "carts/shopping_cart"))}');
$('#cart_container').load('/cart');
setTimeout("$.fancybox.resize()", 200);
setTimeout("$.fancybox.close()", 1200);