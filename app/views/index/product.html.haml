#product_detail.rounded
  .info
    .item_num
      = @product.item_num
      %span.life_cycle= @product.life_cycle
    .title= @product.name
    .price= display_product_price(@product)
    .desc= @product.description.try :html_safe
  .images
    #large_image= image_tag(@product.large_image)
    .left.thumbnail.selected= link_to image_tag(@product.small_image), '#', :onmouseover => "$('#large_image').html('#{image_tag(@product.large_image)}');return false;"
    - @product.images.each do |image|
      .left.thumbnail= link_to image_tag(image.image_url(:small)), '#', :onmouseover => "$('#large_image').html('#{image_tag(image.image_url(:large))}');return false;"
    .clear  
  = link_to 'back', products_path
#product_tabs.rounded
  %ul
    - @product.tabs.each do |tab|
      %li.rounded_top= link_to tab.name, "#tabs-#{tab.id}"
  .clear
  - @product.tabs.each do |tab|
    %div{:id => "tabs-#{tab.id}"}
      == #{tab.id} Sizzlits Decorative Sets are simple to use, compact and portable for creating at home or on the go. Designed to cut a single sheet of cardstock, paper, metallic foil or vellum, these complementary die sets offer an endless array of design possibilities.
:javascript
  $(function() {
  	$("#product_tabs").tabs();
  });