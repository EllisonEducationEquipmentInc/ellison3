%li.product_element
  = link_to image_tag(item.medium_image), send("#{item.class.to_s.downcase}_url", :id => item)
  = link_to item.name, send("#{item.class.to_s.downcase}_url", :id => item)
  - if item.class == Product
    %p= display_product_price(item)