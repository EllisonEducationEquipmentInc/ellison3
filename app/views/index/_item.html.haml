%li.item_element
  %p= link_to image_tag(item.medium_image), send("#{item.class.to_s.downcase}_url", :id => item)
  %p= link_to item.name, send("#{item.class.to_s.downcase}_url", :id => item)
  - if item.class == Product
    %p= display_product_price(item)