%h2 Events
= image_tag "/images/ui-objects/_#{current_system}_banner_event.png"
- unless is_uk?
  = image_tag "/images/ui-backgrounds/events_pins.png", :class => "eventstitleimg"
%h3 Want to connect with us live and in person?
- unless is_uk?
  %p Find us at our next Tradeshow or Convention Booth at a city near you. Simply check our listing below to browse our complete schedule of Upcoming Events for 2011!
- if @events.present?
  %table
    %thead
      %tr
        %th Name
        %th City
        %th State
        %th Country
        %th Event Starts
        %th Event End
    %tbody
      - @events.each do |event|
        %tr
          %td= link_to event.name, event_path(event)
          %td= event.city
          %td= event.state
          %td= event.country
          %td= l event.event_start_date, :format => :date
          %td= l event.event_end_date, :format => :date
  :javascript
    initialize_tables();
- else
  %strong
    %em No upcoming events scheduled at this point of time. Please visit us later.