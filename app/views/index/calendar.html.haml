%h2{:style=>"text-align:center"} Teacher’s Planning Calendar
- if @calendar_content.present?
  #current_visual_assets= render @calendar_content.visual_assets.current
  .clear
#event_calendar= event_calendar.html_safe