%li.ui-menu-item{:id => "tag_#{tag.id}"}
  = link_to_function "x", "$('#tag_#{tag.id}').css('color', 'gray');$.ajax({url:'/admin/ideas/remove_tag?tag_id=#{tag.id}&id=#{idea_id}'})"
  = tag.name
  == (#{tag.tag_type.humanize})
