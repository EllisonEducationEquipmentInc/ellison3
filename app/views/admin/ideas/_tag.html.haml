%li.ui-menu-item{:id => "tag_#{tag.id}"}
  = link_to_function tag.name, "$('#tag_#{tag.id}').css('color', 'gray');$.ajax({url:'/admin/ideas/remove_tag?tag_id=#{tag.id}&id=#{idea_id}'})", :class => "remove_tag"
  %span.keyword== (#{tag.tag_type.humanize})
