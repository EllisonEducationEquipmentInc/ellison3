- if @campaign.save
  == $("#campaign_#{@campaign.id}").html("#{escape_javascript render('campaign', :campaign => @campaign)}");
- else
  == alert('#{escape_javascript @campaign.errors.full_messages.join("\n")}');